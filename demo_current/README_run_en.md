# OSAG Engineered Demo V2 Run Guide

1. Keep the internal folder structure unchanged.
2. Open `index.html` directly, or use `open_demo.bat` / `open_demo.ps1`.
3. No training, no backend, and no network are required.
