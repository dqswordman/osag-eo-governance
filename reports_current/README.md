# Reports Current

This folder contains current-facing report copies that are still useful after cleanup.

Primary location for current organization and publish reports:

- `../reports/`

This folder keeps only the most relevant current report copies so the main report directory stays focused.

